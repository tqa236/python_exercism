name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@master
      with:
        python-version: '3.7'
    - name: Run tests
      run: |
        python3 -m pip install pytest
        ./test.sh
